# Docker Support for Gemini Review Pipeline v2

Runs the 5-stage AWN4 review pipeline (`batch_runner.py` → `run_review.sh` → `gemini -p`) inside a locked-down Docker container.

## Architecture

```
Host                              Container (gemini-reviewer-v2)
─────────────────────────         ──────────────────────────────
prepared/          ──mount──►     /workspace/prepared  (ro)
spec/              ──mount──►     /workspace/spec      (ro)
review_instructions.md ─mount──► /workspace/review_instructions.md (ro)
db_reference.md    ──mount──►     /workspace/db_reference.md (ro)
run_review.sh      ──mount──►     /workspace/run_review.sh (ro)
batch_runner.py    ──mount──►     /workspace/batch_runner.py (ro)
batch_status.py    ──mount──►     /workspace/batch_status.py (ro)
arabic_dict.db     ──mount──►     /data/arabic_dict.db (ro)
output/reviews_gemini_pipeline_v2 ─► /output (rw)
```

## Security

- **Egress firewall**: Default-deny iptables policy; only `generativelanguage.googleapis.com`, `aiplatform.googleapis.com`, `oauth2.googleapis.com`, `accounts.google.com`, `sentry.io`, and `statsig.com` are whitelisted.
- **Read-only mounts**: All inputs are mounted `:ro`. Only `/output` is writable.
- **Credential hygiene**: API key passed via env var; OAuth credentials mounted read-only.
- **Disposable container**: `--rm` flag ensures no state persists after the run.

## Prerequisites

1. Docker installed and running
2. `prepared/` directory generated by `extract_synset_info.py`
3. Arabic dictionary DB at the default path (or set `ARABIC_DICT_DB`)
4. Authentication: `GEMINI_API_KEY` env var or OAuth credentials in `~/.gemini/`

## Usage

```bash
# All synsets, 4 workers (default)
./run_batch.sh

# Custom worker count
./run_batch.sh --workers 8

# Single synset
./run_batch.sh --workers 1 awn4-02592253-n

# Resume interrupted run
./run_batch.sh --resume --workers 4

# Batch file with adaptive concurrency
./run_batch.sh --batch /path/to/synsets.txt --workers 10 --adaptive

# Custom model
MODEL=gemini-3.1-pro-preview ./run_batch.sh --workers 10
```

## Environment Variables

| Variable | Default | Description |
|----------|---------|-------------|
| `GUIDE_DIR` | `~/Desktop/MLProjects/wn-project/.../linguistic_review_guide` | Parent guide directory |
| `PIPELINE_DIR` | `$GUIDE_DIR/gemini_review_pipeline` | Pipeline root |
| `PREPARED_DIR` | `$PIPELINE_DIR/prepared` | Prepared synset data |
| `OUTPUT_DIR` | `$GUIDE_DIR/output/reviews_gemini_pipeline_v2` | Review output directory |
| `ARABIC_DICT_DB` | `~/Desktop/.../arabic-dictionaries/db/arabic_dict.db` | Dictionary SQLite DB |
| `MODEL` | `gemini-3-flash-preview` | Gemini model to use |
| `WORKERS` | `4` | Default worker count |

## Files

| File | Purpose |
|------|---------|
| `Dockerfile` | Container image: Node.js 20, Gemini CLI, sqlite3, python3, firewall tools |
| `entrypoint.sh` | Init firewall → configure auth → exec batch command |
| `init-firewall.sh` | iptables default-deny egress with Google API domain whitelist |
| `run_batch.sh` | Host-side launcher: builds image, mounts volumes, runs container |
