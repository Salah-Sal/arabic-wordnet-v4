# Un-ignore prepared/ for Gemini CLI read_file access.
# .gitignore blocks prepared/ (85K dirs, regenerable), but the reviewer
# agent needs to read evidence.json and synset_info_masked.yaml directly.
!prepared/
*.pyc
__pycache__/
